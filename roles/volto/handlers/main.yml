---
- name: start {{ project_id }}
  systemd:
    name: "{{ project_id }}"
    state: started
    enabled: yes

- name: retart {{ project_id }}
  systemd:
    name: "{{ project_id }}"
    state: restarted
    enabled: yes
